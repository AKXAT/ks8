apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
spec:
  type: NodePort
  selector:
    app: dashboard-service
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30007